import { getConfig } from '../default.config';

export const getPrimaryColor = () => getConfig().primaryColor;

export const getRippleSpeed = () => getConfig().rippleSpeed;

export const getFontSize = () => getConfig().fontSize;
