import { ContentElementsConfig } from './components/content-elements/default/default.config';
// import colors from './components/content-elements/default/vars/colors';

const customConfig: Partial<ContentElementsConfig> = {
  // primaryColor: colors.RED,
};

export default customConfig;
